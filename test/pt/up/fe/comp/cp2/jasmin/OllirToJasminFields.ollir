import io;
A {
    .field private a.i32;
    .construct A().V {
    invokespecial(this, "<init>").V;
    }

    .method public x().i32 {
        a.i32 :=.i32 10.i32;
        invokestatic(io, "print", a).V;
        ret.i32 a;
    }

    .method public static main(args.array.String).V {
        a.A :=.A new(A).A;
        invokespecial(a.A, "<init>").V;
        invokevirtual(a.A, "x").V;
        ret.V;
    }
}